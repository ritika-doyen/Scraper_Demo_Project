# services:
#   - type: web
#     name: scraper-demo-project
#     env: python
#     buildCommand: |
#       pip install -r requirements.txt
#       playwright install chromium
#       python validate_plugins.py
#     startCommand: python app.py
#     plan: free
#     region: oregon
  


  services:
  - type: web
    name: scraper-demo-project
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      export PLAYWRIGHT_BROWSERS_PATH=/tmp/playwright-cache
      playwright install chromium
    startCommand: python app.py
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /tmp/playwright-cache

